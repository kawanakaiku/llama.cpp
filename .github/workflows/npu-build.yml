name: NPU litertlm build

# Intel NPU (Lunar Lake) 向けの .litertlm を焼いて Release に上げる。
# upterm の対話セッションは勝手に切れるので、全部このジョブの中で完結させる。

on:
  workflow_dispatch:
    inputs:
      model_size:
        description: "Qwen3 のサイズ"
        type: choice
        default: "0.6b"
        options: ["0.6b", "1.7b", "4b"]
      mode:
        description: "normal=既定の書き出し / npu_friendly=DUSとHLFBを潰した書き出し"
        type: choice
        default: "npu_friendly"
        options: ["npu_friendly", "normal", "export_hf"]
      kv_cache_max_len:
        description: "KV キャッシュ長（実質のコンテキスト長）"
        default: "1280"
      kv_dtype:
        description: "KV キャッシュの型。fp16 は転送量が半分になる"
        type: choice
        default: "float32"
        options: ["float32", "float16"]
      weights_int4:
        description: "重みを int4 チャンネル単位にする（サイズ半減を狙う）"
        type: choice
        default: "False"
        options: ["False", "True"]
      ov_config:
        description: "OpenVINO の追加プロパティ 例: CACHE_MODE=OPTIMIZE_SIZE"
        default: ""
      no_prefill:
        description: "prefill 署名を作らない（モデルがほぼ半分になる）"
        type: choice
        default: "False"
        options: ["False", "True"]
      export_args:
        description: "export_hf への追加引数 例: --use_rope_composite=True"
        default: ""
      hf_model:
        description: "export_hf 用の HF モデル ID"
        default: "google/gemma-4-E2B-it"
      hf_name:
        description: "export_hf 用の出力名"
        default: "gemma-4-E2B-it"
      tag:
        description: "Release のタグ"
        default: "npu"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4

      - name: ディスクを空ける
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          df -h /

      - name: ビルド
        shell: bash
        run: |
          set -o pipefail
          mkdir -p /home/runner/build
          cp litert/ci/aot_lnl_linux.py litert/ci/fix_tied_lm_head.py \
             litert/ci/convert_npu_friendly.py /home/runner/build/
          if [ "${{ inputs.mode }}" = "npu_friendly" ]; then
            # 重みと参照メタデータだけ用意する。通常ビルドまでやると 4B は量子化で OOM(143)。
            bash litert/ci/build_npu_litertlm.sh "${{ inputs.model_size }}" \
                 dynamic_int8 128 "${{ inputs.kv_cache_max_len }}" True True "-base" 1 2>&1 | tee build.log
            bash litert/ci/run_npu_friendly.sh "${{ inputs.model_size }}" "-nodus-nohlfb" "${{ inputs.kv_cache_max_len }}" 128 "${{ inputs.kv_dtype }}" "${{ inputs.weights_int4 }}" "${{ inputs.ov_config }}" "${{ inputs.no_prefill }}" 2>&1 | tee -a build.log
          elif [ "${{ inputs.mode }}" = "export_hf" ]; then
            # 公式の export_hf 経路。AOT まで統合されていて gemma-3n/4 系も通る。
            cp litert/ci/patch_gemma4.py litert/ci/export_hf_run.py /home/runner/build/
            bash litert/ci/run_export_hf.sh "${{ inputs.hf_model }}" "${{ inputs.hf_name }}" "${{ inputs.kv_cache_max_len }}" 128 dynamic_wi8_afp32 "${{ inputs.export_args }}" 2>&1 | tee build.log
          else
            bash litert/ci/build_npu_litertlm.sh "${{ inputs.model_size }}" \
                 dynamic_int8 128 "${{ inputs.kv_cache_max_len }}" 2>&1 | tee build.log
          fi
          ls -la /home/runner/my-artifact/

      - name: ログ
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ inputs.model_size }}-${{ inputs.mode }}
          path: build.log

      - name: リリース
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          name: "Qwen3 for Intel NPU (Lunar Lake)"
          body: "litert-torch 書き出し → aot_compile(IntelOpenVINO_LNL) → litert-lm-builder。1.9GB ずつ分割。"
          files: /home/runner/my-artifact/*
